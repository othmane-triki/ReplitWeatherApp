## Packages
date-fns | Date formatting for weather forecasts
recharts | Visualizing temperature trends

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Use OpenMeteo API for weather data (no key required).
Use OpenMeteo Geocoding API for city search.
Icons handled by lucide-react (already in stack).
